name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Install pnpm
        # Version comes from the "packageManager" field in package.json
        uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa # v4

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Process MDX files
        run: pnpm run postinstall

      - name: Type check
        run: pnpm exec tsc --noEmit

      - name: Build documentation site
        run: pnpm run build

      - name: Run ESLint
        run: pnpm exec eslint app components lib mdx-components.tsx eslint.config.mjs next.config.mjs postcss.config.mjs source.config.ts

      - name: Check for broken internal links
        run: |
          echo "🔍 Scanning MDX files for broken internal link patterns..."
          ISSUES=0

          for file in $(find content/docs -name "*.mdx"); do
            if grep -Pn '\]\(\s*\)' "$file"; then
              echo "⚠️  Empty link found in $file"
              ISSUES=$((ISSUES + 1))
            fi
            if grep -Pn '\]\(/[^)]*\s[^)]*\)' "$file"; then
              echo "⚠️  Link with spaces found in $file"
              ISSUES=$((ISSUES + 1))
            fi
          done

          if [ "$ISSUES" -gt 0 ]; then
            echo "❌ Found $ISSUES link issues — review the warnings above"
            exit 1
          else
            echo "✅ No broken internal link patterns found"
          fi

      - name: Extract external links from docs
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          root = Path("content/docs")
          output = Path("external-links.txt")
          pattern = re.compile(r'https?://[^\s)"]+')

          urls = set()
          for file in root.rglob("*.mdx"):
            text = file.read_text()
            urls.update(pattern.findall(text))

          output.write_text("".join(f"{url}\n" for url in sorted(urls)))
          print(f"Extracted {len(urls)} external links to {output}")
          PY

      - name: Check external links (lychee)
        uses: lycheeverse/lychee-action@8646ba30535128ac92d33dfc9133794bfdd9b411 # v2
        with:
          args: >-
            --no-progress
            --include-fragments
            --exclude 'localhost'
            --exclude '127.0.0.1'
            --exclude 'PLACEHOLDER'
            --exclude 'discord.gg'
            --exclude 'git.genesisrnd.com'
            --exclude 'git.door43.org'
            --exclude 'lff.api.languagetechnology.org'
            --exclude 'codexeditor.app'
            --suggest
            'external-links.txt'
          fail: true
          output: lychee-report.md

      - name: Upload link check report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: lychee-link-report
          path: lychee-report.md
          retention-days: 14

      - name: Summary
        run: |
          echo "🎉 CI completed successfully!"
          echo "✅ Dependencies installed"
          echo "✅ MDX processed"
          echo "✅ Type checking passed"
          echo "✅ Site built successfully"
          echo "✅ Linting completed"
          echo "✅ External link checking completed (see lychee-link-report artifact for details)"
          echo ""
          echo "Your documentation site is healthy! 🚀"

  audit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Install pnpm
        uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa # v4

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Fails on high/critical advisories. If upstream advisories with no
      # released fix start blocking unrelated PRs, relax with
      # `continue-on-error: true` rather than deleting the job.
      - name: Dependency audit
        run: pnpm audit --audit-level high